name: Build and Release

# Grant the GITHUB_TOKEN permission to write repository contents.
permissions:
  contents: write

# This workflow runs when you push a tag matching the pattern v*.*.*
on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Check out the repository.
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Set up Java (JDK 11 in this example).
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'

      # 3. Compile the Java source code from the 'src' directory.
      - name: Compile Java Source
        run: |
          # Compile all .java files under src/ and output the classes into the designated IntelliJ output directory.
          javac -d out/production/position-size-caulculator $(find src -name "*.java")
          echo "Compilation output:"
          ls -R out/production/position-size-caulculator

      # 4. Package the compiled classes into a runnable JAR.
      - name: Package into JAR
        run: |
          # Adjust the main class name as needed (include package name if applicable).
          jar cfe PositionSizeCalculator.jar PositionSizeCalculator -C out/production/position-size-caulculator .
          echo "JAR contents:"
          jar tf PositionSizeCalculator.jar

      # 5. Create a GitHub Release using the pushed tag.
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      # 6. Upload the JAR file as a release asset.
      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./PositionSizeCalculator.jar
          asset_name: PositionSizeCalculator.jar
          asset_content_type: application/java-archive
